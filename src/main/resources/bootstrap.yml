server:
  port: 5247
spring:
  profiles:
    active: dev
  application:
    name: yun-auth
  cloud:
    nacos:
      config:
        name: yun-auth
        server-addr:
        group:
      discovery:
        server-addr:
